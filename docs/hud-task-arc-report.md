# A任务弧接入与B位置小图

用户已确认A为主方向、适当吸收B。F-HUD-TASK-ARC-04，base `56f94e697845a61a4015a4561d28192807c39c25`，产品 `c2bc98e4e17e417bbe16cc67d29399898244360a`，原目录/codex/restore-observation-experience；根会话唯一作者，子代理只读。此轮完成已有HUD体验打磨，不改变相机运动、天体时间、地形或资产。

常驻采用银白无框浅弧，左侧地点/阶段，中间一个主读数，右侧当前操作。下降与HOLD显示离地高度，上升使用新段，地表切真实方位；弧不独立推进计时。落区选择进入操作侧，完整时间、科普和仪表继续按需展开。1920/2560/1440宽的实测内容98px、含渐变110px；窄屏下降/地表含渐变156px，窄屏返轨后带落区选择为204px。均保留44px操作热区。

B只吸收展开态小图：观星“位置”面板提供系统关系透镜，地表“详情”提供北向上的实际视向扇区及经纬度、眼高。没有增加常驻胶囊、雷达扫描、燃料或假速度。观星默认用实际场景投影的薄椭圆，而非把天体硬排在样张的弧线上；只保留少量相关节点，完整系统留展开面板。图外机位注明边缘指示。视向扇区仅示意方向，不是相机FOV或测绘范围。

## 语义与反例

- 准备阶段不显示规划器默认高度。显式前往落区由引擎发布只读目标id/名称，避免转场期间sites=[]、availability无siteId造成目标名丢失；不是根据同一目标天体猜“正在绕行”。取消后旧标志不再发布。
- 任务弧与高度、地理位置分开：下降和上升为各自导览段；HOLD保持进度、视向仍能变化。普通转场允许规划器重置当前段进度，不用历史最大值掩盖重规划。完成线使用同一二次曲线子段，终点与指针重合。
- 地表方位读取frame.surface，不读取可能陈旧的控制参数。359°到0°刻度只移动1°；缺测不画假北向。旧地表书签、跨阶段面板关闭、焦点恢复和仅太空载具策略保持。

## 验证

378/378测试（55文件）、tsc与build通过。新增6项涵盖明确绕行/普通重取景、准备占位、HOLD/返程分段、重规划重置、跨北向、薄图点线共比例。构建保留已有fs/path外置和大chunk提示。

冻结JS `index-p3DVOu2K.js`，SHA-256 `6ecde9f2eb4792e6ee6e10e368d149b0dec32ef372999dc0bdb7498a61570694`；CSS `index-CTwyNB9b.css`，SHA-256 `529223c08a06bd5ffbb8ba0301e6b0fb3ffddefcd99dc4fb3aa36f314a45f2ea`。取证期间未重建dist。

最终月球静海/火星耶泽罗两套正常UI旅程均完成，各81项断言与布局记录，共45张图；页面/控制台/资源错误均0。覆盖观星、全景及书签、转场取消、显式白昼、约30km HOLD、天体暂停与导览区分、环顾/仰望、返轨、折叠与Escape、详情和位置图，含1920/2560/1440及390×844/844×390视窗。额外绕行控制7项/4图通过：1920/1440/1280/1024观星操作可点击且不溢出，正常拖动露出背面落区、绕行站名/阶段保留、期间无机库、中途取消不重启、位置图展开与Escape恢复焦点；错误0。最终合计49张图和3份报告。

作者实际看最终火星08/08c、最终月球08/14/16；同体系只读视觉复核看火星08/12/16/19。A的无框浅弧与主读数明确，B只在展开面板成为较大图，未见控件互相覆盖。其余最终帧仅作自动行为证据，视觉NOT_OBSERVED。06-preparing取图接近阶段边界，不宣称完整准备期视觉接受。地面远处横向断片、模糊和粗糙石块继续另列，HUD取证不代表地形已修。

证据根目录`D:\solar-evidence\hud-task-arc`：iteration1为首次实现（弧较宽、单位未细分、未保留绕行目标名称）；final-mars/final-moon/final-controls才绑定最终候选。manifest验证九个源码/测试文件、JS/CSS和截图/报告哈希，浏览器配置不入库。此前载具候选的正常UI截图可作为旧HUD参考，不声称与本轮同机位像素对照。

复验：build并启动preview；PowerShell设置TEST_URL、EVIDENCE_DIR、SITE_ID=jezero或tranquility-base，串行运行`node scripts/verify-hud-phases.mjs`；再运行`node scripts/verify-hud-arc-controls.mjs`。最终用`node scripts/write-hud-task-arc-evidence.mjs`核对绑定。未重跑全部六站、资产故障注入、真实手机触摸/GPU或性能专项。

作者自验与同工具只读意见不代替跨工具批准，独立批准仍BLOCKED，用户整体体验待复玩；仅推任务分支，不合main/部署。保留后续小项：天体时间▷与“继续下降”相邻（已有悬浮说明），展开位置面板后默认小图稍重复。回退revert产品提交c2bc98e后重建；其他作者文件原样保留。
