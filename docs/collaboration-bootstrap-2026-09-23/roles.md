# 按需岗位入口

每次只读本岗位、活跃队列当前卡、相关契约源码。完整背景在原 Pro 包，不要求日常反复阅读。

## zcode：现在的执行主力

继续当前已批准单元，按队列预分配任务串行推进。你拥有实现和自测责任，可以选择更好实现，发现Pro方案错误用最小反例纠正。不要以“只有我可用”为理由自授独立批准。行为/数据验证、画面自检、独立接受分别报告。留下checkpoint，结束当前单元后可领取下一READY支持任务，不反复问用户同一授权。

## Anti：恢复后的第一轮

先读 `D:\Game Zac\Personal Projects\太阳系漫游\review-exports\coordination\queue.md`。优先处理Q待审候选，核对完整SHA、数据指纹、真实UI录像和实际场景；不要按旧brain计划重写已经完成的P1/P2。没观察到的维度写BLOCKED，不从截图亮度或作者措辞推断正确。

审查时不改业务代码。需要修复则列最小问题交作者，或明确接过新任务并退出原评审身份；新修复要重新独立审。积压收口后领取一张视觉任务，先确认单位/时间/uniform/资产契约，使用独立worktree，不碰zcode的活跃目录。

## 独立审查者

核对base/candidate，说明实际看过哪些代码/数据/画面，至少一条反例。返回APPROVE/REQUEST_CHANGES/BLOCKED，明确评审维度与版本。参与写过待审代码就列入作者，不再充当该维度的唯一独立批准者。同工具新会话的反证有价值，但不冒充跨工具。

## zcode：短时值班集成

仅在队列指定你且取得唯一集成所有权后操作候选；先核对作者、审查会话、B/T/C和证据。可以机械执行合并，不能豁免验证。无就绪候选时结束值班，不常驻轮询。第一次需要独立集成会话但没有可验证调用能力时，给用户一条启动消息，不能假装已经唤醒。

## Codex / Pro：按事件或阶段介入

Codex输入限精确问题、B/C、数个相关文件、复现与证据、已排除假设和两个方案；无需全仓再审。Pro输入为固定公开分支提交、前基线、短证据索引和最多3个问题。未合并任务分支也可供审查，不必为让Pro看代码先合main。Pro不能读取的本机图片应先整理为可访问的必要证据，不能声称其已看过。
