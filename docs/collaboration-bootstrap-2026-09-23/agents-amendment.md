# 根 AGENTS 的最小增补（检查点后才应用）

不替换整份 AGENTS。保留当前语言、D盘、相机、HUD、真实性、测试、提交与授权条款。

将“Antigravity 是主要实现工具”那一条更新为：

> zcode 与 Antigravity 是日常主力，按任务分别承担工程/数据与视觉/体验；当前 Anti 额度暂停时由 zcode 继续已批准工作。Codex 只在关键系统判断和疑难问题介入，Pro 做阶段深审。具体流程见 `docs/review-workflow.md`。

并入以下短约束；与原条款重复时合并，不再追加一份：

- 开工先核对任务ID、作者会话、目录、分支和base。动态任务只认 `D:\Game Zac\Personal Projects\太阳系漫游\review-exports\coordination\queue.md`，由指定值班会话串行维护；不要在各worktree复制新看板。
- 一任务一位活跃作者；按任务用短分支/worktree，禁止操作其他活跃作者目录或进程。共享单位/时间/接口先定语义，再并行实现。
- 作者自测与独立批准分开；值班集成不能自授例外。审查绑定准确base/candidate/资产，候选改变需重验。适用独立审查缺失时保留候选，不以额度不足把BLOCKED改成PASS。
- 分别报告代码存在、行为验证、视觉接受、阶段体验接受。待视觉验收生产切片最多2个；不能看图时记NOT_OBSERVED，不能把像素非黑当作视觉接受。
- 领取预分配且依赖满足的下一任务可直接推进，不反复问已授权事项。上下文/额度临界前留下checkpoint；没有就绪任务则记录阻塞并结束，不常驻轮询。

这些是流程条款，启用时单独列出变更；不要静默弱化原始验收或公开授权边界。
