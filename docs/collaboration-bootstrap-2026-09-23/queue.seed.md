# 本机协作队列（种子，未启用）

本文件不是活跃看板。初始化目标：`D:\Game Zac\Personal Projects\太阳系漫游\review-exports\coordination\queue.md`。目标存在则读取合并，禁止覆盖。

workflow_version: solar-coordination-v1
activation_time: 未启用
coordinator_session: 由当前 zcode 填真实会话标识，未知不能虚构
active_code_owner: zcode，当前原目录作者；接手时核对
original_workspace: D:\Game Zac\Personal Projects\太阳系漫游
last_confirmed_head: 7de3a68313f219afb1e45eb983510341c88326c9（仅准备时快照）
stable_experience_commit: 未独立确认；不可直接用最新HEAD冒充
anti_available: false（恢复时实测更新，无日期自动解锁）
codex_default: 不参与日常重度实现/合并/轮询
production_visual_pending_count: 待启用核对，P1如缺独立视验则计入；上限2
integration_owner_and_lease: 未分配；并行前指定唯一会话
runtime_processes_ports_profiles: 未核实，当前作者在checkpoint填入

## 状态索引（唯一在此维护）

| ID | 工作 | 作者 | 准备状态 | 风险 | 下一动作 |
| --- | --- | --- | --- | --- | --- |
| E-P2-CLOSE | 接续当前月表/下降单元，收口真实实现及证据 | 当前zcode | 待用实际checkpoint映射；若已有任务ID沿用原ID | H候选，高程/下降语义变化需按diff确认，不能自行降级 | 已做则验收，未做则接续；不得把P2从头重做 |
| D-P2-EVIDENCE | 当前切片独立数据验证/证据准备，可持续的支持工作 | 同一zcode，E单元结束后串行领取 | 待核对已有覆盖；条件满足后READY | N；改变证明政策另为H | 不依赖Anti，先做最缺的一项独立来源或失败路径 |
| Q-P2-REVIEW | 冻结候选的技术、数据、视觉与必要架构复核 | 非作者会话；Anti恢复先领视觉，H交适配的Pro/Codex | BLOCKED，尚无冻结候选与真实审查者 | 沿用被审风险，不降低 | 有候选即准备短证据包，无审查能力不伪造批准 |

## E-P2-CLOSE 任务卡

用户结果：真实月表站点可停驻、环顾、按当前已批准范围下降/暂停/返回；图像、高度和碰撞来源一致。以当前轮实际计划为准，不通过此卡扩大当前授权。

现场证据：准备时已有 `scripts/fetch-apollo17-window.mjs`、`src/surface/RasterTerrainSource.ts`、`src/world-support/descentCurve.ts` 和引擎/高度/下降未提交改动。当前 base/head/候选、数据文件、完成比例、实际测试由作者补录。

写入范围：当前已拥有的P2文件和相关测试/局部接线；按真实diff列明函数和资产，不笼统开放整个项目。CameraController/契约/高度datum若需改，显式纳入本轮单作者范围；不与Anti同时改热点。

验收：真实DEM与正射的来源/hash/覆盖/datum/NoData；独立样点；图形与碰撞同源；负高程不被球壳遮住；1.7m净空；至少一条真实UI进入和返回；中断/复访/书签；本轮未实现的步骤明确不接受。不要只测最终状态字符串。

已有P1问题仅作相关回归/契约核对：同帧姿态和物理太阳方向是否真正驱动渲染，固定相位/显示几何是否掩盖差错。不是无限重审P1；确认实际回归才建最小修复卡。无法在当前契约内修则升级。

交付：小范围冻结候选、实际证据、失败/未测、下一步；技术/数据/视觉/架构四列分别填写。是否已提交到main如实记录，不能倒推有独立批准。

## D-P2-EVIDENCE 任务卡

目的：Anti未恢复期间继续产出能降低后续返工的材料，避免堆更多待看图功能。

领取前：检查E当前交付、已有测试和源记录；已有覆盖不重复做。一次只选下面一个缺口，记录base和范围：

1. 对当前实际DEM/正射做至少3个独立采样/配准控制点，覆盖边界与NoData，保留原产品元数据和哈希；预期不能由被测插值函数自己生成。
2. 为当前真实进入→停驻→环顾→退出/复访链补失败/取消证据或不接入生产的独立测试夹具，不靠直接调用私有API代替UI。
3. 整理固定候选的录像/代表图和运行时状态指纹、原始测试结果、公开可读的短索引；看不到的图不加画面结论。

默认仅新增独立验证材料/夹具/数据报告，不动全局时间、相机语义、共享runner或测试阈值。不安装大批工具、不下载整星全球数据；如要获取已有批准来源之外的大资产或付费服务另说明具体必要性。

支持任务也有完成点；不得把准备资料变成无限忙碌。完成一个缺口后更新队列，再核对是否可审当前候选或继续另一个已列缺口。不能把未审上游接进稳定体验。

## Q-P2-REVIEW 任务卡

输入：E实际冻结的base/head/candidate、数据hash、实现卡和一份短证据索引。缺少任何必需输入先BLOCKED，不按文件名猜版本。

审查者只读业务代码；读报告之外至少验证一个能证伪实现的反例。技术/数据重点：单位/参考系/高程基准、有效覆盖、取消/晚到、真实UI；视觉重点：正确站点、地平线、接近/暂停/环顾/返回的连续性、地球方向与大小、模式说明。未观察到的维度标BLOCKED/NOT_OBSERVED。

当前另一zcode短会话可补技术反证，但不能满足跨工具或作者自批例外。Anti恢复后优先对精确候选做视觉及相关代码独立复核；Pro可基于公开固定分支与证据作适用架构/技术判断。无人可审时E冻结、D继续。

## 检查点与完成登记

只链接 `templates/checkpoint.md` / `review.md` 对应的实际记录，不复制长聊天。每条更新带时间与操作者。候选更换、依赖变动、资产变动要使受影响旧批准失效。工作区/角色移交须原作者停写与接手确认。

合并完成后归档任务与精确提交，依赖解锁；视觉接受/用户体验接受单独记录。待视验达到2个时，不创建第三个生产体验切片。
