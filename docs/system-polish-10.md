# 系统体验收口10：按用户确认顺序推进

base `26bf7edc73aa9bba0da4ccc4afbba2f606a21169`，目录为主工作区，分支 `codex/restore-observation-experience`。Codex根会话唯一代码作者，子代理只读调查/复核。

1. F-JEZERO-DISTANT-FRAGMENTS-03：当前生产包正常UI选择白昼、暂停天体时间、下降/停驻/环顾。固定机位逐层诊断。先证实断片所属网格，再修接合；不靠隐藏远景或补光消除症状，不动源DEM和相机高度。
2. 地形与石块：检查远近几何实际共边，程序石块按显示三角面定位及朝向；坡度过滤必须消费合成梯度，形态改进仍标示意，不虚构实测石块位置。相机继续消费原始高程，不由显示采样反向改物理数据。
3. 首次切换：当前包冷/热对照，分别观察资源、JS主线程、首次着色器编译，保留正常UI导航可打断性；实测有瓶颈才改，不重写已修复相机路径。
4. 既选五款载具接入：沿用06–09素材、可靠性与姿态约定；先精确比例标定和模型展示层隔离。Apollo完整CSM+LM仍需可靠源包。适用独立批准与待视觉容量保持原状态，不将本轮作者检查自授为稳定main批准。

任务动态状态只在唯一queue。验证重资产统一D:/solar-evidence；过程base诊断不冒充修复后证据。每个子目标冻结候选再进入下一个，保留其他作者文件，完成可推任务分支，不部署。

## 地平线调查

- 当前包已复现默认225°方位的远处长条悬空轮廓。北向环顾345°没有同一显著症状，因此第二次诊断特意保留默认方位；两次证据分别存储，不把无症状机位当修复。
- 只读CPU审计发现MOLA外缘采样失败回零、抽稀边界与声明窗口差一格、解析球与实际粗球弦面不合等风险；尚需同机位隔离证明与本症状的因果关系。

### 第一项实现与证据

同机位隐藏MOLA主体后长带消失；隐藏外裙圈/底盖不变。真实三角面CPU采样：225°方位12km处旧内裙圈比已恢复的L1孔边低112.02m；二维重叠、三维上下分离。修复让MOLA记录实际删面孔界，复用共边桥接，端圈包含全部DTM和MOLA边缘顶点；保留原光照、影像低通延伸、源DEM和相机。

- 定向测试9/9：三站边缘误差分别0.113/0.102/0.141m，法线误差<1.1e-7，无反面；盖尔细化/维多利亚354°经度覆盖；月面已有桥接回归通过。
- 构建通过；保留既有大包及fs/path浏览器external警告。生产包`index-agR2qua8.js`。
- 正常UI耶泽罗白昼→低空HOLD→停驻→环顾→仰视→返轨，6图、错误0。作者看225°修前/修后，长带及中央细缝消失，未以换机位/隐藏远景代替修复。证据`D:/solar-evidence/surface-detail-10/bridge-jezero`，修前`baseline-facing-225`。
- 维多利亚/盖尔同流程正串行复验；未完成前不宣称通过。桥接每站增加约3.2万三角，尚未给性能接受结论。
- 回退：revert本项提交；旧函数保留用于反例。外裙圈的潜在误差不属于本次因果链，未顺手改动。

所有视觉结论为作者自检，同工具只读CPU复核不替代跨工具独立批准；不合main/部署。
