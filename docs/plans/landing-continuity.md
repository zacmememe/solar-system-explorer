# 中止下降与换落区连续性

用户授权修复下降中返回仍继续下降、换落区脱离后跳位。base e38620d，分支 codex/restore-observation-experience，Codex根会话唯一作者。子代理只读。

已定位：返航固定50km目标，高于此处中止会继续下降；ASCENDING完成清空轨迹后同帧仍写默认站点位置，再触发默认flyTo。换落区需核对精确终点球坐标、准备期选时和同侧/远侧入口。

不变量：CameraController唯一写相机；由当前实际位置/姿态开始，返航高度不下降、不回跳落点；同天体绕区不穿球，保持用户可打断；准备/加载失败不抢镜头；HUD读取已生效状态；导览不冒称真实航天动力学。

验证：旧生产包正常UI复现；月火高/低空中止、HOLD与转头、完成大步长反例；球外/跨经度/中途改目标；最终生产包正常UI截图与逐帧位置/姿态证据（未录制完整视频）。证据D:\solar-evidence\landing-continuity。独立批准与作者自测分开，不合main/部署。实施与最终检查已完成，详见[报告](../landing-continuity-report.md)。
