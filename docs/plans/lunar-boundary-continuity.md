# 静海网格边界连续性

任务 F-LUNAR-BOUNDARY-CONTINUITY；用户要求继续深入推进，作者 Codex；base 9db620b5cab5452ff2e64a715fa81e17bd0a0351，原任务分支。独立批准未发生，沿用现有缺陷收口范围。

可推翻的假设：LOLA 外裙边落在数学球面上，而外球是 128×64 的折线网格；中间采样点凸出实际边界，且均匀射线未必包含外矩形角点。单独 computeVertexNormals 又让连接边的法线不同。近处 DTM 折面可能另有 Float32 坐标量化导致法线噪声，先以双精度位置对照验证。

保持不变量：不改变物理 datum、真实 DEM/正射数据、NAC 核心顶点、相机与碰撞/时钟；修复只能触及静海外过渡带几何与显示法线。逐角度采样确保内圈贴实际 L1 边、外圈贴实际月球网格边，包含双方所有拐点，不用透明叠面掩盖缝隙。其他站点外裙边暂不切换算法。

执行：正常 UI 基线 → 边界误差/法线反例 → 最小几何修复 → 生产构建正常下降/HOLD/地面/返轨；单独控制机位的诊断图须标明，不冒充用户操作。同机位比较须存相机/时间；仅高度接近的正常 UI 两轮图不得称逐像素严格 A/B。月面共享法线若修改，回归三站与相关数学测试；不同时运行两个 GPU 验证负载。

证据 D:\solar-evidence\lunar-boundary-continuity\。回退产品提交可完整恢复，原始资产保持不变。完成后记录产品 commit、构建指纹、正常 UI 检查与实际看图范围；用户与独立视觉接受单列。

追加已证实根因：ground-isolation 同机位实验中，法线精度改变未消除近地斜线；仅改 MoonMaterial 视线为 modelViewMatrix 的相机相对向量后斜线消失，纹理/几何/光照保持不变。纳入同一显示连续性修复，不变更权威相机、时钟、物理数据；最终重建后验证静海完整旅程与月球轨道。另两站仍为 MeshStandardMaterial，已完成共享法线路径回归。
