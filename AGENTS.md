# 本项目协作约定

- 使用中文沟通。先读 `README.md`、相关源码和当前已确认的任务方案。分工与日常交接见 `docs/review-workflow.md`。
- zcode 与 Antigravity 是日常主力，按任务分别承担工程/数据与视觉/体验；当前 Anti 额度暂停时由 zcode 继续已批准工作。Codex 只在关键系统判断和疑难问题介入，Pro 做阶段深审。具体流程见 `docs/review-workflow.md`。
- 开工先核对任务 ID、作者会话、目录、分支和 base。动态任务状态只认 `D:\Game Zac\Personal Projects\太阳系漫游\review-exports\coordination\queue.md`，由指定值班会话串行维护；不在各 worktree 复制新看板。
- 一任务一位活跃作者；同一工作目录同一时间只保留一个代码写作者，按任务用短分支/worktree，禁止操作其他活跃作者的目录或进程。保留其他工具或用户尚未提交的修改；不要自动覆盖、清理或回滚。共享单位/时间/接口先定语义，再并行实现。
- 一轮只处理当前目标。避免顺便重写框架、升级依赖或修改部署配置。
- 基础体验修复与交接遵守 `docs/experience-foundations.md`：先用正常 UI 复现，再给出可推翻的根因和反例；zcode 不代写未观察的视觉结论，Anti 不以视觉补丁改共享空间/相机语义。新增通过结论必须绑定本轮实际候选与证据。
- 相机由 `CameraController` 统一控制；HUD 消费引擎状态，不能另行推进模拟时钟或控制相机。保持交互可打断，避免空闲自动回正。
- 明确区分物理量、展示比例和示意。程序纹理不得称为探测器照片；资产保留来源与许可记录。
- 分别报告代码存在、行为验证、视觉接受与阶段体验接受；测试通过、失败和未执行分开，没看过的图记 NOT_OBSERVED，不能把像素非黑当作视觉接受。编译成功不能证明 WebGL 画面、交互或性能已验收。
- 作者自测与独立批准分开；值班集成不能自授例外。审查绑定准确 base/candidate/资产，候选改变需重验；适用独立审查缺失时保留候选并如实标 BLOCKED，不以额度不足降为 PASS。待视觉验收的生产切片最多 2 个。
- 领取预分配且依赖满足的任务可直接推进，不反复请求已确认的授权；上下文/额度临界前留下 checkpoint；没有就绪任务则记录具体阻塞并结束，不空轮询、不擅自扩到 32 天体。
- 提交前查看差异与文件清单；排除凭据、浏览器配置、依赖、构建产物、本地备份和导出包。不要执行无检查的 `git add .`。
- 审查交接应包含 commit、范围、源码证据、执行顺序、验收和回退方法。方案保存在 `docs/`，不只留在聊天中。
- 用户已授权将本 GitHub 仓库公开；部署、再次变更可见性或改写 Git 历史仍需对应授权。
- 本机可自定义位置的软件、依赖、缓存和临时文件优先放 D 盘；沿用现有工具，不为审查额外安装浏览器或模型。项目目录与 Google Drive 同步：过程录像、截图、原始数据缓存等重资产不得存放在项目目录内，统一放 `D:\solar-evidence\`（验证脚本默认已指向该处，可用 EVIDENCE_DIR/DEM_CACHE_DIR 覆盖）；`review-exports/coordination/` 队列目录除外（仅文本）。
